# 元组  不可变的序列，类似列表，但创建后不可修改
# 特点： 可以存储不同类型的元素
#       元素可以重复，有序，不可修改（支持索引访问、切片）



# 定义为
# 元组名称 = （元素...）
# 定义空元组
# 元组名称 = ()/tuple()

# 常用方法
count() :统计元素在元组中出现的次数
index() :查找某个元素在元组中的索引位置